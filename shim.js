if (typeof BigInt === "undefined") global.BigInt = require("big-integer");


//Demostartionn of using the BigInteger
/**
 * check if the BigInt is Present and Valid in node package itself if its There means just Leave It 
 * 
 * if(typeof BigInt == undefined ) global.BigInt = require("bigint Library")
 * 
 */